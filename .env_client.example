# Client 环境变量配置示例
# 复制此文件为 .env_client 并填入实际值

# Solana 网络: solana-localnet, solana-devnet, solana
SVM_NETWORK=solana-devnet

# Solana RPC URL (可选，如果不设置将使用默认值)
SVM_RPC_URL=https://api.devnet.solana.com

# 用户私钥 (Base58 格式)
# 可以使用 solana-keygen new 生成
USER_SVM_PRIVATE_KEY=your_private_key_here

# 需要支付的资源 URL (可选，默认为 http://localhost:4021/weather)
NEED_PAY_RESOURCE_URL=http://localhost:4021/weather
