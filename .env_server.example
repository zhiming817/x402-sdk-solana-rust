# Server Configuration

# Server address that will receive payments (Public Key)
ADDRESS=your_server_public_key_here

# Payment network (solana-localnet, solana-devnet, solana)
NETWORK=solana-devnet

# Facilitator service URL for payment verification
FACILITATOR_URL=http://localhost:3002

# Optional: Token configuration (leave empty to use SOL)
TOKEN_MINT_ADDRESS=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v  # USDC on devnet
TOKEN_DECIMALS=6
TOKEN_NAME=USDC

# Server host and port
HOST=127.0.0.1
PORT=4021

# Payment amount per request (in smallest unit, e.g., lamports or token decimals)
PAYMENT_AMOUNT=100000  # 0.1 USDC
